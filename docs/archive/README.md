# Archive

Files that no longer apply to the current system, kept for the record. Nothing in here is load-bearing, and nothing in here is linted — the living documents are the ones `consistency-checks.py` watches.

| File | What it was | Why it is here |
|---|---|---|
| `t3.mjs` | A throwaway Playwright smoke test for `design-docs.html` | One-off scratch tooling; the reader is regenerated by `build_reader.py` and checked by eye |
