# Brand assets

The master identity board — the source of truth for `../brand-identity.md` — belongs here as `vectus-brand-board.png`. Dylan holds the original; it was delivered in-session on August 18, 2026 and has not yet been committed (the session that documented it could only see it, not save it). When it lands, re-sample the ⚠-marked color values in `brand-identity.md` from the actual pixels before any of them become CSS tokens.

Nothing in this folder ships to players directly; shipped art follows the no-AI rule.
